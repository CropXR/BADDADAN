# BADDADAN

Predict _A. thaliana_ expression of gene modules.

Installation requires attached conda environment. Also the following are needed for amici:
```
sudo apt install libatlas-base-dev
sudo apt install libhdf5-serial-dev
```